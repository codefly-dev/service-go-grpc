replacements:
  - from: Web
    to: "{{ .Service.Name.Title }}"
  - from: base_replacement
    to: "{{ .Service.Name.DNSCase }}"
ignores:
  - .codefly.yaml
  - service.generation.codefly.yaml
  - go.work
  - go.work.sum
  - buf.lock
